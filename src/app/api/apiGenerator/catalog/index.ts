import { _sectionsCatalogApi } from "./sectionsCatalogApi";

/** В этом месте усиливаем наши точки и добавляем к ним функционал по необходимости
 *  (автоматически сгенерированные точки не трогаем) */

const sectionsCatalogApi = _sectionsCatalogApi.enhanceEndpoints({});

export { sectionsCatalogApi };
